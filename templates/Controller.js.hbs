exports.getAll{{pascalCase name}} = (req, res) => {
    res.status(200).json({ message: 'Get all {{camelCase name}}' });
};

exports.create{{pascalCase name}} = (req, res) => {
    res.status(201).json({ message: 'Create {{camelCase name}}' });
};